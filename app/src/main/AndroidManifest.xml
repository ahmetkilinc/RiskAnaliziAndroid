<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.radsan.yldrmdankorunma_riskanalizi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CevreActivity" />
        <activity android:name=".ServisActivity" />
        <activity android:name=".OnlemActivity" />
        <activity android:name=".KayipActivity" />
        <activity android:name=".KayipActivity2" />
        <activity android:name=".SonucActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".TumAnalizlerActivity" />
        <activity android:name=".AyarlarActivity" />
        <activity android:name=".ProfilimiDuzenleActivity" />
        <activity android:name=".HakkindaActivity"></activity>
    </application>

</manifest>